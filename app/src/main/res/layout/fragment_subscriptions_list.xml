<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".SubscriptionsListFragment"
    android:orientation="vertical"
    android:background="@color/lightLightGrey"
    >

    <TextView
        android:id="@+id/txt_no_sub"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/no_sub"
        android:textColor="@color/darkDarkGrey"
        android:textSize="18sp"
        android:layout_gravity="center"
        android:layout_margin="15dp"
        />

    <ListView
        android:id="@+id/subscriptions_list_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"
        android:background="@color/lightLightGrey"
        android:clipToPadding="false"
        android:scrollbarStyle="outsideOverlay"
        >
    </ListView>

</LinearLayout>